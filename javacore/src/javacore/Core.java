package javacore;

public class Core {
                 //variable **variable is a container used to store a value
                           //  variable addresses the memory location there three types instance local and static
 
}
